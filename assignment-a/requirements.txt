# Assignment A calculators + CLI use the Python standard library only —
# no install needed to run `cli.py`.
#
# The extras below are for the later layers of Assignment A:
#   pytest            -> run the unit tests
#   anthropic         -> LLM-phrased assistant (Claude Haiku)
#   python-dotenv     -> load ANTHROPIC_API_KEY from .env
pytest>=8.0
anthropic>=0.40
python-dotenv>=1.0
